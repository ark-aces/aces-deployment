server {
    server_tokens off;

    listen 80;
    server_name {{ domain_name }};

    location /.well-known/acme-challenge {
        root /var/www/{{ app_service_name }}_letsencrypt;
        try_files $uri $uri/ =404;
    }

    location / {
        rewrite ^ https://{{ domain_name }}$request_uri? permanent;
    }
}
